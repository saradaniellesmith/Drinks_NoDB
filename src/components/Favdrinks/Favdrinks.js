import React, {Component} from 'react';

class Favdrinks extends Component {
    constructor(props) {
        super(props);
        this.state = {
            favDrinks = []
        }
    }

const Favdrinks = (props) => {
    let savedResults = props.favDrinks.map((curr, i) => {
        return 
    })






componentDidMount() {
    axios.get("/api/saveDrink")
    .then(response => {

    })
}



}

